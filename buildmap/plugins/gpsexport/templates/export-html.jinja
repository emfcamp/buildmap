<!DOCTYPE html>
<html>
<head>
    <title>{{name}} Map exports</title>
</head>
<body>
<ul>
    {% for name, types in files.items() %}
    <li>{{name}} {% for type in types%}<a href="/{{type}}/{{name}}.{{type}}">{{type}}</a> {% endfor %}</li>
    {% endfor %}
</ul>
</body>
</html>


server {
        listen 80 default_server;
        listen [::]:80 default_server;

        root /home/vagrant/map.emfcamp.org;

        index index.html;

        server_name _;

        location / {
                try_files $uri $uri/ =404;
                autoindex on;
        }
}
